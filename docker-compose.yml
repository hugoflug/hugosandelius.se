version: '3.2'
services:
  hatify:
    image: hugosandelius/hugosandelius.se:hatify
    restart: always
    volumes:
      - static:/static
  tagga:
    image: hugosandelius/hugosandelius.se:tagga
    restart: always
  nginx: 
    image: hugosandelius/hugosandelius.se:nginx 
    restart: always
    ports: 
      - "80:80"
    volumes:
      - static:/static
volumes:
  static: